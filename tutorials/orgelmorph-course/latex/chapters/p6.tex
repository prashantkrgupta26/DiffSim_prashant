\chapter{Allen--Cahn crystallization}
\label{ch:p6}

\begin{keybox}
\textbf{Key idea.} Crystallinity is a \emph{non-conserved} order parameter
--- crystal can be created, unlike composition --- so it obeys
\emph{Allen--Cahn} (non-conserved gradient flow), not Cahn--Hilliard. A
crystal grows below the melting point and melts above it; the way its
fraction advances in time is the Avrami (JMAK) law.
\end{keybox}

\section{The model}

Let $\psi\in[0,1]$ be the local crystallinity of a crystallizable species.
Its non-conserved gradient flow is
%
\begin{equation}
  \dd{\psi}{t} = -L_\psi\Big[\dd{f}{\psi} - \varepsilon^2\grad^2\psi\Big],
\end{equation}
%
with the crystallization free energy (the 2310.11844 ``r14'' form,
PCBM-class energetics)
%
\begin{equation}
  f_{\text{cr}} = \phi\big[q(\psi)\,\Delta\sigma + p(\psi)\,\text{drive}\big],
  \quad q=\psi^2(1-\psi)^2,\quad p=\psi^2(3-2\psi),
\end{equation}
%
and the Turnbull undercooling driving force
%
\begin{equation}
  \text{drive} = \Delta h\,(T/T_m - 1).
\end{equation}
%
$q(\psi)$ is a double-well barrier separating amorphous ($\psi=0$) from
crystalline ($\psi=1$); $p(\psi)$ interpolates the driving; $L_\psi$ sets
the growth rate; $\varepsilon^2$ the crystal-interface width.

\begin{keybox}
\textbf{The melting point sets the sign.} $\text{drive}\propto(T/T_m-1)$
is \emph{negative} for $T<T_m$ (so $\psi\to1$ lowers $f$: the crystal
\emph{grows}) and \emph{positive} for $T>T_m$ (the crystal \emph{melts}).
$T_m=\PsixTm$~K here (materials.yaml, PCBM class). $\Delta h$ is the
crystallization enthalpy, $\Delta\sigma$ the barrier height, $L_\psi$ the
kinetic prefactor, $\varepsilon^2$ the gradient penalty (crystal
interface width $\sim\sqrt{\varepsilon^2}$).
\end{keybox}

\section{Growth kinetics: the Avrami law}

With athermal nuclei placed at $t=0$ growing at a roughly constant
interface velocity $v$, the crystalline area of each grain grows like
$(vt)^2$ in 2-D before grains impinge, so the crystalline \emph{fraction}
follows the Avrami/Johnson--Mehl--Avrami--Kolmogorov (JMAK) law
%
\begin{equation}
  X(t) = 1 - \exp\!\big[-(kt)^n\big],
\end{equation}
%
with Avrami exponent $n$ reporting the growth dimensionality and
nucleation mode. For \emph{point} nuclei appearing over time with 2-D
growth, $n\approx3$; for \emph{pre-existing} 2-D nuclei, $n\approx2$;
and \emph{finite, pre-placed} supercritical seeds (what we use, because
point seeds are sub-critical and redissolve) give an effective exponent
\emph{below} $2$ --- the seeds start with area, so the early $t^2$
regime is foreshortened. We fit $n$ from the measured $X(t)$ via the
double-log linearization $\ln[-\ln(1-X)] = n\ln t + \text{const}$ over
the pre-impingement window.

\section{Run it}

\begin{runbox}
\textbf{Run it.}
\begin{lstlisting}
cd physics/06_allen_cahn_crystallization
python run.py            # grow/melt + Avrami fit
\end{lstlisting}
It seeds a single crystal below and above $T_m$ (growth vs melting) and a
multi-nucleus run whose fraction is fit to the Avrami law. Compare with
\file{EXPECTED.md} and the figures.
\end{runbox}

\paragraph{What you should see.} Below the melting point ($T=333$~K) the
seeded disc \emph{grows} (crystalline area $\PsixGrowStart\to\PsixGrowEnd$,
a factor $\PsixGrowRatio$); above it ($T=700$~K) it \emph{melts}
($\PsixMeltStart\to\PsixMeltEnd$). The multi-nucleus run reaches
crystalline fraction $X=\PsixAvramiXend$ from $\PsixSeeds$ nuclei
($\PsixGrains$ grains resolved), and the fitted Avrami exponent is
$n=\PsixAvramiN$ --- an intermediate value below the ideal $n=2$ because
the nuclei are finite pre-placed seeds (the exercises explore how the
nucleation mode moves $n$).

% --- generated by gen_figures.py ---
\begin{figure}[h]\centering
  \includegraphics[width=\linewidth]{figures/p6_grow_melt.png}
  \caption{A seeded crystal grows below $T_m$ and melts above it (left:
    area vs time; right: the final $\psi$ fields). The driving force
    changes sign at the melting point.}
  \label{fig:p6growmelt}
\end{figure}

\begin{figure}[h]\centering
  \includegraphics[width=\linewidth]{figures/p6_avrami.png}
  \caption{Avrami/JMAK kinetics: the sigmoidal crystalline fraction
    $X(t)$ (left), the double-log linearization whose slope is the Avrami
    exponent (middle), and the grains coloured by orientation $\theta$
    with the crystal outline (right).}
  \label{fig:p6avrami}
\end{figure}

\begin{keybox}
\textbf{Measured self-check.}
\begin{center}\small
\begin{tabular}{lc}
\toprule
grow ($T<T_m$): area start $\to$ end & $\PsixGrowStart \to \PsixGrowEnd$ (\PsixGrowRatio$\times$) \\
melt ($T>T_m$): area start $\to$ end & $\PsixMeltStart \to \PsixMeltEnd$ \\
Avrami exponent $n$ & \PsixAvramiN \\
crystalline fraction $X_{\text{end}}$ & \PsixAvramiXend \\
nuclei $\to$ grains resolved & $\PsixSeeds \to \PsixGrains$ \\
\bottomrule
\end{tabular}
\end{center}
\end{keybox}

\section{Code walk-through}

\paragraph{1. The crystallizing stepper.} \code{MultiPhaseStepper(dm,
M=1, K=1, ..., bulk="r14")} adds one crystallinity field $\psi$ (and its
orientation $\theta$) to one composition. \code{dsig}, \code{dh},
\code{Tm}, \code{eps2}, \code{L\_psi} are the coefficients above.

\paragraph{2. Seeding a crystal.} \code{set\_initial([phi], [psi\_disc],
[theta])} places a $\psi$ disc (a tanh profile) --- the athermal nucleus
--- with an orientation marker.

\paragraph{3. The Avrami fit.} \code{\_fit\_avrami} linearizes $X(t)$ over
the growth window ($0.05<X<0.9$) and reads $n$ as the slope; the grains
come from \code{grain\_labels} (a $\theta$-watershed).

\section{Explore on your own}

\question{Vary the undercooling $T/T_m$. Does the growth velocity (and so
the Avrami $k$) increase monotonically as you cool further below $T_m$,
or is there a maximum? Why might very deep undercooling slow growth?}

\question{Change the number of pre-placed nuclei. Does the Avrami
exponent $n$ stay near 2 (fixed nuclei) or move toward 3 (if nucleation
kept happening during growth)? What does $n$ tell you about the
nucleation mode?}

\question{Vary $\varepsilon^2$ and measure the crystal interface width.
Confirm the $\sqrt{\varepsilon^2}$ scaling, and relate the width to the
mesh spacing --- when is the crystal interface under-resolved?}

\question{Give two seeds \emph{different} orientation markers $\theta$ and
let them grow into contact. Do they merge or form a boundary? (This is
the coupled orientation dynamics of Chapter~\ref{ch:p7}.)}

\question{The crystal expels the amorphous component as it grows (the
$\phi$ field). Measure the composition inside vs outside the crystal ---
this is the crystallization-driven demixing that Chapter~\ref{ch:p7}
makes central.}

% ====================================================================
