# Chapter 00 smoke — GPU variant. Identical physics; runs on cuda:0.
# (The monolithic saddle still uses splu at this tiny size; --solver auto picks
# it as the documented small-problem fallback.)
level: 3
Re: 100
dt: 0.05
nsteps: 20
device: cuda:0
precision: fp64
