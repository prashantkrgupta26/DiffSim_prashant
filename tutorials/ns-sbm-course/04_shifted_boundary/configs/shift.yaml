# Chapter 04 — flow past a body with a genuine SBM shift (0 < dmax < h).
# offset=0.05 gives dmax/h=0.80 at level 4 — a real sub-cell shift. Reference
# mode reproduces the drag comparison in EXPECTED.md.
level: 4
Re: 40
offset: 0.05
alpha: 10.0
dt: 0.02
nsteps: 600
device: cpu
precision: fp64
modes:
  quick:
    nsteps: 300
  reference:
    nsteps: 600
  research:
    level: 5
