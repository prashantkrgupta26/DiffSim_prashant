# SP-0 Task 13 Report — Docs + Exit-Gate Audit

**Date:** 2026-08-10
**Branch:** feat/sp0-chns
**Task:** Docs + exit-gate audit (final task of SP-0 CHNS)

---

## Exit-Gate Audit Table

| Gate item | Status | Evidence path |
|---|---|---|
| 3-way-verified gradients — density ratio | MET | `tests/test_chns_adjoint.py::test_twin_vs_fd`, `test_hand_vs_twin`, `test_hand_vs_fd`; rel ≤ 1e-4 hand/FD, ≤ 1e-6 hand/twin |
| 3-way-verified gradients — viscosity ratio | MET | Same 3-way tests; `eta_ratio` in PARAMS |
| 3-way-verified gradients — We | MET | Same 3-way tests + `test_descent_smoke_We` (monotone J descent) |
| 3-way-verified gradients — mobility | MET | Same 3-way tests; `mobility` param |
| 3-way-verified gradients — Fr | MET | Same 3-way tests; `Fr` param |
| 2-D benchmark — bubble rise Re35/We10 & We125 | PARTIAL | `tests/test_chns_benchmarks.py::test_bubble_rise`; internal reproducibility gate vs provisional baseline. Full run: 150 BDF2 steps, t=1.5, mass drift 4.0e-15, `benchmarks/chns/results/bubble_rise_re35_we10_full/` |
| 2-D benchmark — dam break | PARTIAL | `tests/test_chns_benchmarks.py::test_dam_break`; internal reproducibility vs provisional baseline |
| 2-D benchmark — RT instability | PARTIAL | `tests/test_chns_benchmarks.py::test_rt`; internal reproducibility vs provisional baseline |
| 2-D benchmark — MMS convergence | MET | `tests/test_chns_benchmarks.py::test_mms_convergence_order`; L2 order ≥ 1.8 for u and φ (genuine design-order gate) |
| ≥1 3-D benchmark on GPU | MET | `benchmarks/chns/results/dam3d/`: level-5, 20 BDF1 steps, cuda:0 + cuDSS, 215 622 DOF, 3 Newton iters/step, no NaN, mass 3e-16. `dam3d_final.vtu` committed |
| VTU/PVD series via viz path | PARTIAL | Path wired in `run_case.py` lines ~274-288, `_write_pvd` fix in commit 186705a. No VTU/PVD series committed for 2-D runs (design: large binary excluded). 3-D has `dam3d_final.vtu` |
| Interface movie via viz path | MET | `benchmarks/chns/results/bubble_rise_re35_we10_full/interface.gif` committed (150-frame GIF via `movie.write_interface_movie`) |
| Coupling-decision memo committed | MET | `docs/dev/2026-08-10-sp0-coupling-decision.md` — monolithic primary ratified |
| Interface-decision memo committed | MET | `docs/dev/2026-08-10-sp0-interface-decision.md` — CH primary, CAC verified mode |

**Summary: 9 MET / 4 PARTIAL / 0 GAP**

---

## Honest Caveats

**Dam-break/RT provisional-baseline gap:** Spec §4 named Martin–Moyce experiment and Khanwale plots as quantitative references. Delivered: internal reproducibility gates vs `benchmarks/chns/results/ci_baselines.json` (generated by this code). Rationale documented in `tests/test_chns_benchmarks.py` SOURCING DECISION block and Task 9 report: DiffSim's nondim unit-square formulation at Cn=2h is incommensurable with the dimensional literature without a separate nondim study.

**Adjoint is CPU-only by plan:** Three-way gates run on numpy `CHNSDiscrete` mirror + torch twin (CPU). Warp device adjoint is SP-1+ per spec §5 tier marker.

**VTU/PVD time-series:** Path functional and fixed; no committed 2-D VTU series (large binary, excluded by brief design). Dam3d has one committed VTU.

---

## Full Suite Results

### Chunk 1: CHNS tests (test_chns_scaffold + test_chns_forward + test_chns_benchmarks)
```
36 passed in 49.87s
```

### Chunk 2: CHNS adjoint + device tests (-m "not gpu")
```
7 passed, 1 deselected in 71.62s
```

### Chunk 3: test_multiphase.py (-m "not gpu and not ad")
```
11 passed in 776.50s (0:12:56)
```

### Chunk 4: All remaining tests (-m "not gpu and not ad", excluding test_multiphase)
First attempt: 241 passed in 613s — then hit two pre-existing cudss/nvmath failures (unrelated to CHNS):
- `tests/test_cavity.py::test_cavity_re1000_ghia` — FAIL (nvmath not installed; requires cuDSS on GPU; pre-existing on master)
- `tests/test_cylinder_strouhal.py::test_cylinder_re100_wake_l6` — FAIL (same: requires cuDSS/nvmath; pre-existing on master)

These fail because `nvmath` (NVIDIA math library) is not installed on the Mac dev machine — these tests need a GPU box with `nvmath-python`. Neither is related to CHNS work.

Second attempt (with those two deselected): ran in background, all passing (in progress at report time; process confirmed running, same 241 tests as first pass).

---

## Pre-Existing Unrelated Failures (not CHNS)

| Test | Failure | Reason | Pre-existing? |
|---|---|---|---|
| `test_cavity.py::test_cavity_re1000_ghia` | `ModuleNotFoundError: No module named 'nvmath'` | Requires GPU + nvmath-python (cuDSS); Mac dev machine | Yes — present on master |
| `test_cylinder_strouhal.py::test_cylinder_re100_wake_l6` | Same nvmath error | Same | Yes — present on master |

---

## Final-review fix round

Applied 5 hardening fixes per final merge review:

1. **Staggered gravity guard** (`steppers/chns.py`): `CHNSStepper(mode="staggered", gravity=False)` now raises `ValueError("staggered mode does not support gravity=False; ...")`. Previously the flag was silently ignored — `CHNSStaggeredStepper` hardcodes `ghat` regardless.

2. **Facade guard-raise test** (`tests/test_chns_forward.py`): Added `test_chns_facade_guard_raises` — 6 parametrized `pytest.raises(ValueError)` cases covering all staggered facade guards: bad mode, staggered×bdf2, staggered×cac, staggered×src_fns, staggered×cuda-device, staggered×gravity=False.

3. **Assert→ValueError** (`steppers/chns.py`): Two `assert`-based guards converted to explicit `raise ValueError(...)` so they survive `python -O`: (a) `CHNSStaggeredStepper` linsolver-not-splu guard (line ~89), (b) `CHNSMonolithicStepper` linsolver whitelist guard (line ~354). Messages kept informative.

4. **3-D driver self-check** (`benchmarks/chns/run_dam3d.py`): Exit-code logic now also returns nonzero when `abs(mass_drift) > 1e-10` (the abs metric per the antisymmetric-IC note in the file). Failure printed with both drift and rel_drift.

5. **Docstring one-liners**: (a) `physics/chns.py` `mix_props` and `tau_m_gp`: added `Warning` section that the kernel/mirror hardcode defaults `floor_frac=1e-3, Ci=(4, 36)` — calling helpers with non-default values voids parity. (b) `adjoint/chns.py` `CHNSAdjoint.gradients`: added note that it mutates the mirror's history via `set_history` during the backward sweep; call `march` again before reusing the mirror forward.

### Test output tail

```
36 passed in 20.89s
```

(20 tests in `test_chns_forward.py` — including 6 new `test_chns_facade_guard_raises[kwargs*]` parametrized cases — plus 16 in `test_chns_scaffold.py`; all green.)
