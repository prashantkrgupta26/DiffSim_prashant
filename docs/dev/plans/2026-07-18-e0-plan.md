# E0 Plan — "Thinking Differentiable" pedagogy sequence (E0a–E0c + reference page + LaTeX)

> **For agentic workers:** execute subagent-driven (commit-per-green, agents
> never push). Spec: `docs/dev/specs/2026-07-18-e0-differentiable-pedagogy-design.md`.
> Steps use checkbox syntax for tracking.

**Goal:** the adjoint on-ramp for an FEM-fluent, AD-new group — three runnable
chapters, a citable adjoint-readiness reference page, and a LaTeX companion.

**Architecture:** pure additions over the existing stack (tutorials/ + docs/);
NO solver-code changes (spec §7). Each chapter follows the house contract
(*Learning outcome → Background → documented code → Expected results →
Explore*) and prints reproducible numbers on CPU in ≤ ~3 min.

## Global constraints (bind every task)

- **REPO-IDENTITY GUARD:** work ONLY in
  `/Users/baskarg/Dropbox/work/Projects/ClaudeCode/DiffSim`; the shell resets
  cwd to a different clone — absolute cd prefix on every command;
  `git rev-parse --show-toplevel` must print the Dropbox path before any
  commit. Branch: `e0-pedagogy` (from master). Do NOT push.
- **Attribution** (spec §1): every chapter's Background cites
  dolfin-adjoint/pyadjoint — P.E. Farrell's mathematical background; Farrell,
  Ham, Funke & Rognes (2013); Mitusch, Funke & Dokken (2019) — plus the
  specific adapted device (three-way comparison, mother problem, reaction-rate
  lineage). README carries a consolidated attribution note.
- **Chapter contract:** Expected-results numbers printed by the script and
  stated in the docstring with tolerance bands (≥2× headroom vs measured);
  three-way gradient checks at the 1e-6 class; CPU warp, smallest meshes that
  demonstrate the point (L3/L4 2-D).
- Math call-outs as clearly-marked asides (`# ── math aside ──` blocks) —
  frequent, per Baskar.
- No torch dependencies in E0a/E0c (E0b may import torch only if the
  walkthrough of an existing taped path requires it — prefer not).
- Style: mirror `E1_shape_optimization.py`'s docstring voice and the
  A-track's pedagogy density.

## Tasks

### T1 — E0a: Thinking differentiable  (`tutorials/E_differentiable/E0a_thinking_differentiable.py`)
- [ ] Sections per spec §3-E0a: Farrell-adapted foreword (reduced functional,
      information-flow reversal, adjoint-is-linear); three-ways table (FD /
      tangent-linear / adjoint, cost argument); naked `wp.Tape` toy (≤10-line
      kernel, backward, inspect grads); Poisson dJ/dκ on a small 2-D grid
      (reuse `physics/poisson.py` bricks + splu): forward J = ½Σ(u−u_obs)² on
      probes, adjoint via ONE transposed solve (reuse the factorization —
      say so), dJ/dκ as a GP-field cotangent; the "we did not differentiate
      through splu" seed, flagged for E0b.
- [ ] **Gates:** script runs clean from repo root (CPU, ≤3 min); three-way
      check (adjoint vs wp.Tape-through-residual vs central FD) rel ≤1e-6
      printed; FD-cost-vs-adjoint-cost table printed (n_params solves vs 2);
      Expected-results block in docstring matches printed numbers.
- [ ] Commit `feat(e0): E0a thinking differentiable — three ways to a
      gradient, tape toy, Poisson dJ/dkappa`.

### T2 — E0b: Anatomy of a taped brick  (`E0b_anatomy_of_a_taped_brick.py`)
- [ ] Walkthrough per spec §3-E0b: what `wp.Tape` records on a REAL brick
      (use the Poisson volume kernel path from `physics/poisson.py` /
      `sbm` adjoint machinery as the specimen — quote the actual kernel
      lines in comments and annotate them), requires_grad arrays, custom-VJP
      vs unrolled tape, cotangent entry points, tape scope discipline,
      factory/cache-coexistence note.
- [ ] **The do-not-differentiate list** section (relation-not-algorithm):
      linear solver → transposed solve (same LU); Newton → IFT at the
      converged residual; mesh/carve/classification/preflight → frozen
      (E1's freeze as the worked instance, cross-ref); RNG/tabulation →
      inputs. Each with a 2–4-line runnable or quoted-code illustration.
- [ ] **Compute-once-and-store** section: one factorization → A and Aᵀ
      solves (time it both ways, print the ratio); tabulated basis reuse;
      GP-field precomputation outside the tape ("frozen at GPs", M2
      convention cross-ref).
- [ ] **Dot-product test** section: ⟨Av,w⟩=⟨v,Aᵀw⟩ on the specimen operator,
      printed to machine precision.
- [ ] Payoff: κ(x)-field recovery inverse crime (two-blob κ field, ~40 GD
      steps, plot-free convergence table printed).
- [ ] **Gates:** runs clean ≤3 min; dot-product test ≤1e-12 printed;
      factorization-reuse timing ratio printed; recovery err drops ≥100×
      from init (printed table); Expected-results match.
- [ ] Commit `feat(e0): E0b anatomy of a taped brick — do-not-differentiate,
      compute-once, dot-product test, kappa recovery`.

### T3 — E0c: The recipe  (`E0c_recipe_new_pde.py`)
- [ ] **The checklist** as the chapter spine (spec §3-E0c list verbatim,
      expanded with one-line rationales; marked as "normative twin:
      docs/site theory page" — cross-link).
- [ ] **Transient heat chain**: taped BDF1 march (~30 steps, small 2-D),
      backward sweep, dJ/dκ through time; the primal-storage inventory
      printed (arrays × steps × bytes).
- [ ] **Compute-vs-memory** section: full-store vs recompute-from-checkpoint
      (implement BOTH for the heat chain; print memory + wall for each; the
      backward ≤2.5× forward contract measured and printed; revolve named
      as the scaling path, not built).
- [ ] **Choosing your J** section: runnable mini-demos of (i) a nonsmooth J
      (max-based) giving a jumpy gradient vs its smooth relaxation (logsumexp)
      — printed gradient-noise comparison; (ii) signal-scale check
      (H4 protocol: print |dJ| vs FD noise floor BEFORE optimizing); (iii) a
      flat-J geometry (probe far from the sensitivity support) vs an
      informative placement — printed conditioning/gradient-magnitude
      contrast; (iv) the identifiability aside: beyond-FH gauge story +
      Tikhonov-as-science, told in ~20 lines of prose comments with numbers
      quoted from `docs/dev/2026-07-14-beyond-fh-learned-thermo.md`
      (no rerun); (v) multi-observable J note → SP-1 bridge.
- [ ] **Mini phase-field gradient**: tiny AC grid (reuse
      `physics/allen_cahn.py`), dJ/d(mobility or ε²) via the taped chain,
      three-way checked; "learning reaction rates" lineage attribution;
      forward pointers (F-track, M4, SP-1 R3).
- [ ] **Gates:** runs clean ≤3 min; both storage modes agree on dJ to 1e-10
      (printed); 2.5× contract measured (printed); AC three-way ≤1e-6;
      Expected-results match.
- [ ] Commit `feat(e0): E0c the recipe — checklist, transient chain,
      store-vs-recompute, choosing J, mini phase-field`.

### T4 — Reference page + integration
- [ ] `docs/site_src/theory/adjoint_readiness_checklist.md`: the normative
      checklist + do-not-differentiate list + J-design rules, terse,
      citable; "derived in E0c" cross-link; note that new specs/reviews cite
      this page (supersedes scattered findings-4c citations going forward).
- [ ] README updates: `tutorials/README.md` (E-track table gains E0a–E0c +
      the suggested-paths line for "differentiable-simulation student"
      updated to start at E0a), `tutorials/E_differentiable/README.md`
      (table + attribution note), site nav via `python tutorials/build_site.py`.
- [ ] tutorial-smoke: add E0a + E0c (and E0b if <2 min measured) to the
      cpu-micro tier following the existing workflow pattern.
- [ ] **Gates:** `mkdocs build` (or the repo's site-build command) succeeds
      with new nav; smoke tier passes locally; links resolve.
- [ ] Commit `docs(e0): adjoint-readiness reference page + curriculum
      integration + smoke tier`.

### T5 — LaTeX companion  (`docs/course/e0_differentiable_tutorial.tex`)
- [ ] Farrell-style math background in DiffSim notation (reduced functional,
      TLM vs adjoint derivation, discrete-adjoint stance), the three
      chapters' theory consolidated, checklist appendix; bibliography with
      the attribution set; F-track `m4_phasefield_tutorial.tex` as the
      structural template.
- [ ] **Gates:** compiles with tectonic (gpubox toolchain via remote scripts
      if not local); no overfull-hbox storms; cross-refs resolve.
- [ ] Commit `docs(e0): LaTeX companion — mathematical background +
      checklist appendix`.

## Sequencing & execution notes

T1 → T2 → T3 (narrative continuity; each references the previous) → T4
(checklist finalized by T3) → T5. One implementer at a time (SDD discipline);
tutorials/docs only — zero collision surface with the sp1-r0 branch, but the
E0 chain still queues behind the running R0 work unless Baskar says
interleave. Reviews per task (sonnet), with T3's J-section and T2's
walkthrough getting the closest read (they carry the group's mental model).
