# M5 (a)-pack — realism extensions ledger (A1/A2/A3)

Agent build, 2026-07-12.  Formulation contract: docs/theory/
flow_film_formulation_p2.md Track A (ratified); free energy and
dynamics per docs/theory/crystallization_formulation_p1.md.  Code:
src/diffsim/physics/multiphase.py (module docstring carries the
weak forms); gates: tests/test_multiphase_apack.py.  Style model:
the S2 replication ledger (2026-07-10-m5-s2-16390-replication.md).

## 1. A1 — temperature as a field

DESIGN.  Nodal T(x, t) solved as a SEGREGATED linear step per BDF1
attempt (`TemperatureField`): rho_cp dT/dt = div(k_th grad T) + s,
host-scipy assembly (consistent mass + stiffness, one LU per dt
value), Dirichlet substrate/ambient rows by replacement, insulated
natural BCs otherwise.  SPLITTING CHOICE (recorded): Lie split — T
advances on [t, t+dt] from the committed field FIRST, then the
crystallization step reads T^{n+1} at quadrature points.  Splitting
error O(dt), the same order as BDF1: no temporal-order degradation.
T commits only on ACCEPTED steps (ladder-consistent; a rejected
attempt discards its T solve).

FIELD COUPLING.  In field mode the kernel computes the crystal
driving PER GP (p1: dh(1 - T/Tm); r14: dh(T/Tm - 1)); the host passes
dh in the drive slot + Tm + GP-interpolated T.  Scalar mode keeps the
host-side drive path BIT-IDENTICALLY (compile-time flag; the S0
fixed-dt parity re-measured 8.27e-15 after the kernel revision,
identical to the pre-A-pack lock).  chi(T): NOT parameterized in this
brick (constant chi inputs); a future chi(T) law must read the same
per-GP Tq array (plumbing exists — recorded).  FDT noise amplitude
stays the global calibration knob; spatially-varying kBT(x) noise is
a recorded frontier.  D(T) HOOK: optional Arrhenius factor
exp(-Ea (1/T - 1/T_ref)) on the CH mobility per GP (fastmode lam,
_n-mode Vignes D_i, const-mob Ons), both T modes.  M_psi(T) (WLF on
the AC prefactor) is a recorded follow-up.

GATES (measured 2026-07-12, L4/L5/L6 as noted):

| gate | measured | lock | verdict |
|---|---|---|---|
| (i) T-field MMS (L4->L5, dt 1e-3 x4) | errs 9.53e-4 -> 2.38e-4, order 2.00 | order > 1.8 | PASS |
| (ii) isothermal parity (8 jittered repeats, 40 fixed steps, s1b-class seeded growth) | per-run max 8.66e-15..9.10e-15 | 5e-14 (5.5x tail) | PASS |
| (iii) gradient demo (L6, Dirichlet ramp 300 -> 700 K, Tm = 558 K crossing at x = 0.645, seeds at x = 0.15/0.85, t = 0.6) | psi-mean cold half 0.1678 vs hot half 0.00055 = 304.6x; cold area 0.1048 -> 0.1649 (1.57x); hot psi_max 0.0165; ramp held to < 1e-8 | ratio > 10x; cold growth > 1.2x; hot psi_max < 0.3 | PASS |
| (iv) D(T) hook | inert ratio 1.0 exactly at T_ref = T; active ratio 0.3660 vs Arrhenius 0.3657 (0.1%) | (0.7, 1.4) x mT | PASS |

PARITY NOTE.  The parity gate uses FIXED dt (attempt counts stay
aligned between modes); the ladder-vs-ladder comparison would add the
documented FP-fate coin flips (S0 march gate covers that class).  The
measured distribution is FP-identical class: the segregated T of a
uniform field is uniform to LU roundoff (~1e-16 relative), and the
per-GP drive arithmetic coincides with the host expression.

## 2. A2 — substrate surface energy

DESIGN.  F_w = Int_{Gamma_w} (g_i phi_i + h_i phi_i^2) dS over
retained species; natural-BC term in the mu_i rows (weak form in the
module docstring A2: kap dphi/dn = -f_w'), consistent P1 face mass
host-assembled in full node space (the wodo top-flux pattern),
Jacobian block -2 h_i Mw on (mu_i, phi_i).  Face-generic:
wall_face = (axis, side); default (1, 0) = y = 0.  wall_g = wall_h
= 0 bypasses the machinery entirely (wall_on flag) — the g = 0
regression is the UNMODIFIED code path by construction.

QUADRATIC-TERM PHYSICS NOTE (measured during gate design): f_w' =
g + 2 h phi vanishes at phi* = -g/(2h) — the wall RESTORES phi toward
phi*, so h > 0 with |g| < 2 h phi_bulk DEPLETES a phi_bulk = 0.5 film
(phi* < 0.5) rather than "saturating the enrichment"; the quadratic
gate asserts the restoring direction on both sides of phi*.

GATES (measured 2026-07-12, miscible binary chi = 1.5 < chi_c = 2,
kappa = 1e-3, phi0 = 0.5, march to t = 0.5; near-wall band y < 1/16):

| gate | measured | lock | verdict |
|---|---|---|---|
| (i) enrichment, g = -0.005 (L6) | bottom excess +0.07821, top -0.004723 (mass balance), phi_max 0.6471 (no projection saturation), 0 rejects | bottom > +0.03, bottom > top | PASS |
| (ii) sign flip, g = +0.005 (L6) | bottom -0.07821, top +0.004723 — EXACT mirror (the phi -> 1-phi symmetry of this config) | bottom < -0.03, antisymmetry within 50% | PASS |
| (iii) g = 0 regression (L5, 3 jittered steps) | max dx vs no-arg stepper = 0.0 exactly (same code path) | < 1e-13 | PASS |
| (iv) quadratic restoring (L5, h = 0.02) | phi* = 0.125 (g = -0.005): bottom excess -0.13045 (pull-down); phi* = 0.75 (g = -0.03): +0.08867 (pull-up); 0 rejects both | pull-down < -0.05, pull-up > +0.04 (>= 2x) | PASS |

## 3. A3 — anisotropic crystal growth

DESIGN.  a_k(alpha) = eps_k (1 + delta cos(m (alpha - theta_k))),
flux q = a^2 grad psi + a a' R grad psi (module docstring A3); the
coefficients are Picard-frozen per iterate (residual exact); the
|grad psi| -> 0 singularity is the smooth blend delta_eff =
delta g2/(g2 + a_reg^2).  delta_a = 0 compiles the ISOTROPIC kernel
(identical factory cache key) — the regression gate is exact by
construction.  CONTRACT: frozen-marker theta only (asserted); the KWC
back-torque d f_grad/d theta is the recorded extension.

MEASURED SWEEP (2026-07-12, L6, s1b PCBM r14 config, T = 333 K, seed
r0 = 0.08 at fixed theta = 30 deg, m = 2, march to t = 0.5, 0 rejects
at every delta; node-thresholded second-moment metric):

| delta_a | AR | long axis (deg) | cryst. nodes |
|---|---|---|---|
| 0.0  | 1.0000 | (degenerate) | 177 |
| 0.02 | 1.0429 | 24.7 | 173 |
| 0.05 | 1.0691 | 45.0 (pixel-noise artifact, see below) | 169 |
| 0.1  | 1.1301 | 29.98 | 165 |
| 0.2  | 1.2669 | 27.94 | 165 |

AXIS-METRIC NOTE.  At weak anisotropy the thresholded node set
(~170 nodes) pixel-noises the principal-axis estimate (the 45.00 deg
value is the Ixx ~ Iyy near-degeneracy of the estimator, not
physics); at delta = 0.1/0.2 the axis locks to 29.98/27.94 deg vs
theta = 30 deg.  The in-suite gate uses the smoother PSI-WEIGHTED
moments and gates the axis at delta = 0.1.

GATES (psi-weighted moments, in-suite config; measured 2026-07-12):

| gate | measured | lock | verdict |
|---|---|---|---|
| (i) delta_a = 0 regression (L5, 3 steps) | max dx vs no-delta stepper = 0.0 exactly (identical kernel cache key) | < 1e-13 | PASS |
| (ii) single-seed AR monotone (L6, t = 0.5, theta = 30 deg, m = 2) | ARw 1.0000 / 1.0284 / 1.0726 / 1.1506 at delta 0 / 0.02 / 0.05 / 0.1 (gaps 0.0284 / 0.0442 / 0.0780); 0 rejects | strictly monotone; AR(0.1) - AR(0) > 0.07 (2.15x) | PASS |
| (iii) orientation locking | axis 29.89 deg at every delta > 0 (theta 30); 60.11 deg at theta 60, delta 0.1 — abs err 0.11 deg | < 2 deg per delta and at the second theta | PASS |

## 4. Suite status

A1 stage (2026-07-12, this workstation, cuda:0):
- tests/test_multiphase.py + tests/test_multiphase_apack.py (A1 set):
  15 passed in 1047.7 s.  Slowest: s1b impingement 301 s, s1c anchors
  256 s, a1 isothermal parity 106 s.
- tests/test_multiphase_s2.py minus test_s2c_crystallite_quench_and_
  dissolution (the known multi-hour march; the ONLY deselect): 9
  passed, 1 deselected in 1624.3 s.  Slowest: s2d grain statistics
  455 s, s2c aaps-first 384 s, s2b placement 349 s, s2c growth
  contrast 321 s.
- S0 fixed-dt parity re-measured after the kernel revision: 8.271e-15
  (identical to the pre-A-pack lock value); s2a2 fastmode_n parity
  5.12e-13 (lock 5e-12) — the compile-time A-pack flags leave the
  default paths bit-class intact.  NOTE: an FP-associativity slip
  (Lpsi*(eps2*gpsi) vs the original (Lpsi*eps2)*gpsi) was caught and
  reverted during the build — the isotropic branch keeps the ORIGINAL
  association (comment in the kernel).

A2/A3 stages: gate runs recorded in their commits (same suite set).

## 5. Frontiers recorded

- chi(T) parameterization (reads per-GP Tq when it lands).
- M_psi(T) WLF on the AC prefactor.
- Spatially-varying FDT noise amplitude kBT(x).
- Wall face mass is P1 (p2 = tensor-rule extension).
- Anisotropy requires marker (frozen) theta; the KWC back-torque
  d f_grad/d theta on a dynamic theta is a recorded extension.
